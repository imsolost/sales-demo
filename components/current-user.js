import styled from "styled-components";

const StyledContainer = styled.div`

`;
const CurrentUser = () => {
  return (
    <StyledContainer>
        Sally Jones
    </StyledContainer>
  );
};

export default CurrentUser;
import styled from "styled-components";

const StyledContainer = styled.div`

`;
const CurrentUser = () => {
  return (
    <StyledContainer>
        Mark Daniels
    </StyledContainer>
  );
};

export default CurrentUser;